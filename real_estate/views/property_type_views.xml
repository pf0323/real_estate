<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- View property_type View List -->
    <record id="view_property_type_list" model="ir.ui.view">
        <field name="name">view.property.type.list</field>
        <field name="model">property.type</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
            </list>
        </field>
    </record>

    <!-- View property_type form -->
    <record id="view_property_type_form" model="ir.ui.view">
    <field name="name">property.type.form</field>
    <field name="model">property.type</field>
    <field name="arch" type="xml">
        <form>
            <sheet>
                <group>
                    <field name="name"/>
                </group>
            </sheet>
        </form>
    </field>
    </record>

    <!-- View property_type search -->
    <record id="view_property_type_search" model="ir.ui.view">
        <field name="name">view.property.type.search</field>
        <field name="model">property.type</field>
        <field name="arch" type="xml">
            <search>
                <group expand="1" string="Group By">
                    <filter string="Name" name="name" domain="[]" context="{'group_by':'name'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action property_type -->
    <record id="action_property_type" model="ir.actions.act_window">
        <field name="name">Property Types</field>
        <field name="res_model">property.type</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new property type
            </p>
        </field>
    </record>

</odoo>
